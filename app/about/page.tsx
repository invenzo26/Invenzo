import { AboutSection } from '@/app/components/AboutSection'

export default function AboutPage() {
  return <main style={{ background: 'var(--bg-layered)', color: 'var(--text-primary)' }}><AboutSection /></main>
}
